@model TopMenuModel.CategoryLineModel
<li>
    <a href="@Url.RouteUrl("Category", new { SeName = Model.Category.SeName })">
        @Model.Category.Name
    </a>
    @if (Model.Category.HaveSubCategories)
    {
        Model.Category.SubCategories = Model.Category.SubCategories.Take(7).ToList();
        <ul>

            @foreach (var category in Model.Category.SubCategories)
            {
                var categoryLineModel = new TopMenuModel.CategoryLineModel
                {
                    Category = category
                };
                @await Html.PartialAsync("_CategoryLine.TopMenu", categoryLineModel)
            }
        </ul>
    }
</li>