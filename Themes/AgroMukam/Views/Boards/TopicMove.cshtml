@model TopicMoveModel
@{    
    Layout = "_ColumnsOne";

    //title
    Html.AddTitleParts(T("Forum.PageTitle.MoveTopic").Text);
    //page class
    Html.AppendPageCssClassParts("html-move-topic-page");
}
@await Component.InvokeAsync("ForumBreadcrumb", new { forumTopicId = Model.Id })
<div class="page move-topic-page">
    <div class="page-title">
        <h1>@T("Forum.MoveTopic")</h1>
    </div>
    <div class="page-body">
        <form asp-route="TopicMove" method="post">
            <input type="hidden" asp-for="Id"/>
            <div class="fieldset">
                <div class="form-fields">
                    <div class="form-group">
                        <div class="row">
                            <label class="col-12 col-sm-12 col-md-4 col-lg-4 col-xl-4 col-form-label">@T("Forum.SelectTheForumToMoveTopic"):</label>
                            <div class="col-12 col-sm-12 col-md-5 col-lg-5 col-xl-5">
                                <select class="form-control form-control" asp-for="ForumSelected" asp-items="@(new SelectList(Model.ForumList, "Value", "Text"))"></select>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="buttons">
                <input type="submit" value="@T("Forum.Submit")" class="button-1 btn btn-primary submit-forum-topic-button"/>
                <input type="button" class="button-2 btn btn-default cancel-forum-topic-button" value="@T("Forum.Cancel")" onclick="setLocation('@Url.RouteUrl("TopicSlug", new {id = Model.Id, slug = Model.TopicSeName})')"/>
            </div>
        </form>
    </div>
</div>